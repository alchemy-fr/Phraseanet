<div>
    {% for name, TOU in TOUs %}
        <h1 style="text-align:center;font-weight:bold;font-size:16px;">{{ name }}</h1>
        {{ TOU }}
        {% if loop.last == false %}
            <hr/>
        {% endif %}
    {% endfor %}
</div>